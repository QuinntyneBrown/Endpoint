using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Logging;
using {{ domainNamespace }}.Models;
using {{ applicationNamespace }}.Interfaces;

namespace {{ infrastructureNamespace }}.Data
{
    public class {{ dbContextPascalCase }}: DbContext, I{{ dbContextPascalCase }}
    {
        public {{ dbContextPascalCase }}(DbContextOptions options)
            :base(options) { }
        
        protected override void OnModelCreating(ModelBuilder modelBuilder)
        {

        }
    }
}
